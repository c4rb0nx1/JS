// // 2.11 - logical op

// alert( true || true );   // true
// alert( false || true );  // true
// alert( true || false );  // true
// alert( false || false ); // false

// //imp example
// true || alert("not printed"); // true is returned and not alert because it hit true as the first truth val 
// false || alert("printed"); // alert raised coz it's the first truth encounter.

let ar = ['1','2','3','4'];
console.log('1' in ar);